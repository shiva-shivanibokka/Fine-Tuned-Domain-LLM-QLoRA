# The Modal serverless-GPU backend that runs base vs. fine-tuned inference.
# Deploy serving/modal_app.py with `modal deploy`, then paste the printed
# https://...compare.modal.run URL here. The /api/compare route POSTs to it.
MODAL_COMPARE_URL=https://your-workspace--cuad-legal-llm-cuadmodel-compare.modal.run
