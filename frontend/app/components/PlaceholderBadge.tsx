export default function PlaceholderBadge() {
  return (
    <div className="callout" style={{ marginBottom: "1.25rem" }}>
      Preview data — these numbers are placeholders until the local training and
      evaluation run populates <code>results/</code>.
    </div>
  );
}
