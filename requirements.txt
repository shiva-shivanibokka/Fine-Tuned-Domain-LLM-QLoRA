# ---------------------------------------------------------------------------
# Core training stack. torch>=2.6 is required by trl>=1.x (imports torch FSDP2).
# Install torch with the CUDA build for your GPU, e.g.:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# ---------------------------------------------------------------------------
torch>=2.6.0
transformers>=4.55.0
peft>=0.15.0
trl>=1.0.0
bitsandbytes>=0.45.0        # 4-bit quantization (QLoRA)
datasets>=3.0.0
accelerate>=1.0.0
tokenizers>=0.20.0

# Data pipeline
datasketch>=1.6.0           # MinHash LSH deduplication
numpy>=1.26.0
scipy>=1.13.0

# Evaluation
bert-score>=0.3.13          # BERTScore F1
sentence-transformers>=3.0.0  # embeddings for failure analysis
anthropic>=0.30.0           # G-Eval judge (optional)
openai>=1.35.0              # Groq-compatible client for G-Eval fallback

# Failure analysis
umap-learn>=0.5.6           # UMAP dimensionality reduction
hdbscan>=0.8.38             # density-based clustering
scikit-learn>=1.5.0

# Experiment tracking
mlflow>=2.14.0

# Serving (the FastAPI inference API — deployed to a Hugging Face Space)
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Dev / testing
pytest>=8.0.0
ruff>=0.6.0
