# Core training
torch>=2.2.0
transformers>=4.45.0
peft>=0.12.0
trl>=0.11.0
bitsandbytes>=0.43.0        # 4-bit quantization (QLoRA)
datasets>=2.20.0
accelerate>=0.33.0
tokenizers>=0.19.0

# Data pipeline
datasketch>=1.6.0           # MinHash LSH deduplication
numpy>=1.26.0
scipy>=1.13.0

# Evaluation
bert-score>=0.3.13          # BERTScore F1
sentence-transformers>=3.0.0  # embeddings for failure analysis
anthropic>=0.30.0           # G-Eval judge (optional)
openai>=1.35.0              # Groq-compatible client for G-Eval

# Failure analysis
umap-learn>=0.5.6           # UMAP dimensionality reduction
hdbscan>=0.8.38             # density-based clustering
scikit-learn>=1.5.0

# Experiment tracking
mlflow>=2.14.0

# Serving
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0

# UI
gradio>=5.0.0
plotly>=5.22.0
pandas>=2.2.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
